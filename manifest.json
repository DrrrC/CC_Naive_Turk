{
  "manifest_version": 2,

  "name": "Naive Turk",
  "description": "This extension allows the user to save survey long text and browse previously saved survey answers.",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "NT.html"
  },
  "permissions": [
    "activeTab",
    "storage",
    {"fileSystem": ["write", "retainEntries", "directory"]}
  ]
}
